name: Sonar

# on:
#   workflow_run:
#     workflows: [CI]
#     types: [completed]

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  shared:
    uses: pantos-io/ci-workflows/.github/workflows/sonar.yml@1540b699f0ca4fbc37f6fc2296eacb7b61a846e1
    secrets: inherit
